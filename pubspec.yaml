name: torrent_remote_center
description: A new Flutter project.
publish_to: "none"
version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.2
  flutter_riverpod: ^2.4.0
  dio: ^5.3.2
  flutter_secure_storage: ^9.0.0
  file_picker: ^10.2.0
  shared_preferences: ^2.2.2
  app_links: ^6.4.0
  uuid: ^4.5.1
  flutter_localizations:
    sdk: flutter
  intl: ^0.20.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  generate: true
